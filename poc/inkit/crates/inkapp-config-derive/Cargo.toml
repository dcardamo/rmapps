[package]
name = "inkapp-config-derive"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "#[derive(Config)] for inkapp-config"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
