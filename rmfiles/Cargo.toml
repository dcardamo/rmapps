[package]
name = "rmfiles"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust reader for reMarkable .rm (v6) scene files; extracts ink strokes"
license = "MIT"

[dependencies]
thiserror = "2"

[dev-dependencies]
zip = "2"
